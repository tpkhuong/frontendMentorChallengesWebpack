import React from "react";
/**
 * change value of a property to the obj we assign to React.createContext does not re-render the component
 * **/
export const BoardTaskRenderContext = React.createContext({});
